# 2.0.0 workflow qualification

Representative workflow qualification is retained from the frozen 1.x
baseline and is re-run for the stable release. The tagged
[`v2.0.0-rc.2` qualification run](https://github.com/ikelaiah/mathlib-fp/actions/runs/35537083361)
passed its Linux and Windows clean-archive jobs. The final `v2.0.0` release
workflow produces the publication artifacts from the stable tag.
